# Alpha Engine runs with NO keys on the default paths: crypto (CoinGecko) and
# US equities (Yahoo) both work on a fresh clone. Keys below only ADD layers.
#
# Discovery: `.env.local` / `.env` are looked up in the current directory, the
# project root, and parent directories (first match per name wins). Variables
# already set in your shell always take priority over anything in these files.

# US macro context blended into equity signals (free key from
# https://fred.stlouisfed.org). Without it, equity scans are trend-only.
FRED_API_KEY=

# Optional Indian broker creds. Breeze is the easiest live path for F&O chain
# fetches; leave these blank if you only use fixtures / cached chains.
BREEZE_API_KEY=
BREEZE_API_SECRET=
BREEZE_CLIENT_ID=
BREEZE_USER_ID=
BREEZE_SESSION_TOKEN=

# Angel One (SmartAPI) is the alternate live F&O path:
#   fetch-chain NIFTY --expiry 2026-07-30 --broker angelone
# Get the access token (a JWT) by logging in via SmartAPI, then set it here.
ANGEL_ONE_API_KEY=
ANGEL_ONE_API_SECRET=
ANGEL_ONE_CLIENT_ID=
ANGEL_ONE_ACCESS_TOKEN=

# Dhan is another live F&O path:
#   fetch-chain NIFTY --expiry 2026-07-30 --broker dhan
# Get credentials from https://dhan.co -> My Account -> API
DHAN_CLIENT_ID=
DHAN_ACCESS_TOKEN=

# Optional CoinGecko Pro key: raises crypto rate limits, never required.
# Keyless CoinGecko + the keyless Binance fallback cover the default path.
COINGECKO_API_KEY=

# Optional OANDA credentials for forex candles (free practice account at
# https://www.oanda.com). OANDA_ENV can be "practice" (default) or "live".
OANDA_API_KEY=
OANDA_ACCOUNT_ID=
OANDA_ENV=practice

# Optional LLM narrator. With none set, a deterministic template writes the thesis.
# Supports any OpenAI-compatible API (OpenAI, Anthropic via proxy, local models).
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini
LLM_API_BASE=https://api.openai.com/v1

# ---------------------------------------------------------------------------
# EXECUTION / LIVE TRADING — all off by default. Read GETTING_STARTED.md Part 6.
# ---------------------------------------------------------------------------

# The master switch. Orders are simulated ("paper") unless this is set truthy.
# Leave blank to stay safe. Set to 1 ONLY when you intend to trade real money.
LIVE_TRADING=

# Hard safety caps — an order past either is rejected BEFORE it reaches a broker,
# even in live mode. Defaults (100 units / 500000 notional) apply if left blank.
MAX_ORDER_QTY=
MAX_ORDER_NOTIONAL=

# Shared password for the inbound trade webhook. The webhook server refuses to
# start without this. Use a long random string.
WEBHOOK_SECRET=

# ---------------------------------------------------------------------------
# Phase 11 context data (all optional, all free tiers)
#
# Without these the engine still runs: the affected analyzer abstains with
# weight 0 and says so. Nothing breaks, you just get a narrower read.
#
# Populate these caches with `./start.sh ingest` (or let `orchestrate` do it).
# The scan path reads them cache-only and never fetches inline.
# ---------------------------------------------------------------------------

# Company-tagged news headlines -> analyzers/sentiment.py
# Free key: https://finnhub.io
FINNHUB_API_KEY=

# Company fundamentals (income/balance/cash-flow) -> analyzers/fundamentals.py
# Free key: https://financialmodelingprep.com
FMP_API_KEY=

# Crypto on-chain flows (exchange netflow, active addresses)
# -> analyzers/crypto_onchain.py. Free tier: https://glassnode.com
# Note: funding rate + open interest come from Binance and need NO key at all,
# so the on-chain analyzer already works without this.
GLASSNODE_API_KEY=

# SEC EDGAR filings and AAPL quarterly fundamentals -> ingestion/rss.py and
# ingestion/sec_fundamentals.py
#
# NOT an API key and NOT a signup. The SEC's fair-access policy simply requires
# every request to identify who is making it, with contact information. Without
# this the feed returns 403 Forbidden on every call — silently, forever.
#
# Format: your name and a real email address.
SEC_USER_AGENT=

# Where the engine keeps everything it writes: cache, signal log, trades,
# calibration, health. Optional.
#
# Default is `data/` relative to wherever you run the command from. That is
# right for the repo flow and wrong if you run `alpha-engine` from another
# directory — it would create a second `data/` folder there and split your
# signal log. Set this to an absolute path to pin it.
#
# scripts/daily.sh sets this itself, so the scheduled job is unaffected.
# ALPHA_DATA_DIR=/Users/you/finance-project-quant/data
